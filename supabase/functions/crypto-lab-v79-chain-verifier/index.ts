import "jsr:@supabase/functions-js/edge-runtime.d.ts";
import { createClient } from "jsr:@supabase/supabase-js@2";

export const VERIFIER_VERSION = "7930-rpc1";
const ORIGIN = "https://1specnazov1.github.io";
const TRANSFER_TOPIC = "0xddf252ad1be2c89b69c2b068fc378daa952ba7f163c4a11628f55a4df523b3ef";
const B58 = "123456789ABCDEFGHJKLMNPQRSTUVWXYZabcdefghijkmnopqrstuvwxyz";

type Profile = {network_code:string;environment:string;provider_code:string;chain_reference_expected:string;public_endpoint:string|null;endpoint_secret_name:string|null;api_key_secret_name:string|null};
type SandboxCase = {id:string;network_code:string;asset_code:string;token_identifier:string|null;recipient_address:string;expected_amount_base_units:string|number};
type Observation = {network_code:string;tx_hash:string;sender_address:string|null;recipient_address:string;asset_code:string;token_identifier:string;amount_base_units:string;block_reference:string|null;finality_status:string;execution_success:boolean;verifier_source:string;observed_at:string};

const now = () => new Date().toISOString();
const clean = (v:unknown,n=180) => String(v??"").replace(/[\r\n\u0000-\u001f\u007f]+/g," ").trim().slice(0,n);
const lowerHex = (v:string) => v.toLowerCase().replace(/^0x/,"");
const cors = (origin:string) => ({"Access-Control-Allow-Origin":origin,"Access-Control-Allow-Headers":"authorization, apikey, content-type, x-client-info","Access-Control-Allow-Methods":"GET, POST, OPTIONS","Access-Control-Max-Age":"86400","Vary":"Origin"});
const reply = (body:unknown,status:number,origin:string) => Response.json(body,{status,headers:{...cors(origin),"Cache-Control":"no-store","X-Content-Type-Options":"nosniff"}});

async function fetchJson(url:string,init:RequestInit,timeout=10000){const ctl=new AbortController();const timer=setTimeout(()=>ctl.abort(),timeout);try{const res=await fetch(url,{...init,signal:ctl.signal});const text=await res.text();if(!res.ok)throw new Error(`HTTP_${res.status}`);return JSON.parse(text||"{}");}finally{clearTimeout(timer);}}
async function rpc(url:string,method:string,params:unknown[],headers:Record<string,string>={}){const data=await fetchJson(url,{method:"POST",headers:{"content-type":"application/json",...headers},body:JSON.stringify({jsonrpc:"2.0",id:1,method,params})});if(data?.error)throw new Error(`RPC_${clean(data.error?.code,20)}_${clean(data.error?.message)}`);return data?.result;}
function endpoint(p:Profile){if(p.public_endpoint)return p.public_endpoint.replace(/\/$/,"");if(p.endpoint_secret_name)return (Deno.env.get(p.endpoint_secret_name)||"").replace(/\/$/,"");return "";}
function headers(p:Profile){const out:Record<string,string>={};if(p.api_key_secret_name){const k=Deno.env.get(p.api_key_secret_name)||"";if(k)out["TRON-PRO-API-KEY"]=k;}return out;}
function assertTx(network:string,tx:string){const ok=network==="ETHEREUM"?/^0x[0-9a-fA-F]{64}$/.test(tx):network==="TRON"?/^[0-9a-fA-F]{64}$/.test(tx):/^[1-9A-HJ-NP-Za-km-z]{64,88}$/.test(tx);if(!ok)throw new Error("INVALID_TX_HASH");}

export function decodeBase58(value:string){let n=0n;for(const ch of value){const i=B58.indexOf(ch);if(i<0)throw new Error("INVALID_BASE58");n=n*58n+BigInt(i);}let hex=n.toString(16);if(hex.length%2)hex="0"+hex;let out=hex?Uint8Array.from(hex.match(/.{2}/g)!.map(x=>parseInt(x,16))):new Uint8Array();let zeros=0;while(zeros<value.length&&value[zeros]==="1")zeros++;if(zeros){const merged=new Uint8Array(zeros+out.length);merged.set(out,zeros);out=merged;}return out;}
export async function tronAddressHex(address:string){const raw=decodeBase58(address);if(raw.length!==25||raw[0]!==0x41)throw new Error("INVALID_TRON_ADDRESS");const body=raw.slice(0,21),checksum=raw.slice(21);const h1=new Uint8Array(await crypto.subtle.digest("SHA-256",body));const h2=new Uint8Array(await crypto.subtle.digest("SHA-256",h1));if(!checksum.every((v,i)=>v===h2[i]))throw new Error("INVALID_TRON_CHECKSUM");return [...body].map(x=>x.toString(16).padStart(2,"0")).join("");}

export function parseEthereumTransfer(receipt:any,tx:any,token:string,recipient:string){if(!receipt)throw new Error("TX_NOT_FOUND");const tokenHex=lowerHex(token),recipientTopic=lowerHex(recipient).padStart(64,"0");const log=(receipt.logs||[]).find((x:any)=>lowerHex(String(x.address||""))===tokenHex&&String(x.topics?.[0]||"").toLowerCase()===TRANSFER_TOPIC&&lowerHex(String(x.topics?.[2]||""))===recipientTopic);if(!log)throw new Error("TRANSFER_LOG_NOT_FOUND");return {sender_address:tx?.from||null,recipient_address:recipient,token_identifier:token,amount_base_units:BigInt(log.data||"0x0").toString(),block_reference:String(receipt.blockNumber||""),execution_success:String(receipt.status||"").toLowerCase()==="0x1"};}
export async function parseTronTransfer(info:any,tx:any,token:string,recipient:string){if(!info?.id)throw new Error("TX_NOT_FOUND");const tokenHex=(await tronAddressHex(token)).slice(2).toLowerCase();const recipientTopic=(await tronAddressHex(recipient)).slice(2).padStart(64,"0").toLowerCase();const log=(info.log||[]).find((x:any)=>lowerHex(String(x.address||""))===tokenHex&&`0x${lowerHex(String(x.topics?.[0]||""))}`===TRANSFER_TOPIC&&lowerHex(String(x.topics?.[2]||""))===recipientTopic);if(!log)throw new Error("TRANSFER_LOG_NOT_FOUND");return {sender_address:tx?.raw_data?.contract?.[0]?.parameter?.value?.owner_address||null,recipient_address:recipient,token_identifier:token,amount_base_units:BigInt(`0x${lowerHex(String(log.data||"0"))||"0"}`).toString(),block_reference:String(info.blockNumber??""),execution_success:String(info?.receipt?.result||info?.result||"").toUpperCase()==="SUCCESS"};}
export function parseSolanaTransfer(transaction:any,mint:string,recipient:string){if(!transaction)throw new Error("TX_NOT_FOUND");const pre=transaction.meta?.preTokenBalances||[],post=transaction.meta?.postTokenBalances||[];let delta=0n;for(const p of post){if(String(p.mint)!==mint||String(p.owner)!==recipient)continue;const b=pre.find((x:any)=>x.accountIndex===p.accountIndex&&String(x.mint)===mint&&String(x.owner)===recipient);delta+=BigInt(p.uiTokenAmount?.amount||"0")-BigInt(b?.uiTokenAmount?.amount||"0");}if(delta<=0n)throw new Error("TOKEN_DELTA_NOT_FOUND");const keys=transaction.transaction?.message?.accountKeys||[];return {sender_address:typeof keys[0]==="string"?keys[0]:keys[0]?.pubkey||null,recipient_address:recipient,token_identifier:mint,amount_base_units:delta.toString(),block_reference:String(transaction.slot??""),execution_success:transaction.meta?.err===null};}

async function checkHealth(p:Profile){const started=performance.now(),url=endpoint(p);if(!url)return {ok:false,latency_ms:0,error_code:"ENDPOINT_UNCONFIGURED",checked_at:now(),evidence:{provider_code:p.provider_code,read_only:true}};try{if(p.network_code==="ETHEREUM"){const chain=BigInt(await rpc(url,"eth_chainId",[])).toString();const head=await rpc(url,"eth_blockNumber",[]);let finalized="";try{finalized=String((await rpc(url,"eth_getBlockByNumber",["finalized",false]))?.number||"");}catch{}return {ok:chain===p.chain_reference_expected,latency_ms:Math.round(performance.now()-started),chain_reference_observed:chain,latest_block_reference:String(head),finality_reference:finalized,error_code:chain===p.chain_reference_expected?null:"CHAIN_REFERENCE_MISMATCH",checked_at:now(),evidence:{provider_code:p.provider_code,read_only:true}};}
if(p.network_code==="TRON"){const data=await fetchJson(`${url}/wallet/getnowblock`,{method:"POST",headers:{"content-type":"application/json",...headers(p)},body:JSON.stringify({visible:true})});const block=String(data?.block_header?.raw_data?.number??"");if(!block)throw new Error("INVALID_TRON_HEAD");return {ok:true,latency_ms:Math.round(performance.now()-started),chain_reference_observed:"nile",latest_block_reference:block,finality_reference:String(data?.blockID||""),error_code:null,checked_at:now(),evidence:{provider_code:p.provider_code,read_only:true}};}
const health=await rpc(url,"getHealth",[]);const genesis=await rpc(url,"getGenesisHash",[]);const slot=await rpc(url,"getSlot",[{commitment:"finalized"}]);return {ok:health==="ok",latency_ms:Math.round(performance.now()-started),chain_reference_observed:"devnet",latest_block_reference:String(slot),finality_reference:String(genesis),error_code:health==="ok"?null:"SOLANA_UNHEALTHY",checked_at:now(),evidence:{provider_code:p.provider_code,read_only:true}};}catch(e){return {ok:false,latency_ms:Math.round(performance.now()-started),error_code:clean(e instanceof Error?e.message:e),checked_at:now(),evidence:{provider_code:p.provider_code,read_only:true}};}}

async function verifyCase(p:Profile,c:SandboxCase,txHash:string):Promise<Observation>{assertTx(c.network_code,txHash);if(!c.token_identifier)throw new Error("TOKEN_IDENTIFIER_UNCONFIGURED");const url=endpoint(p);if(!url)throw new Error("ENDPOINT_UNCONFIGURED");if(c.network_code==="ETHEREUM"){const receipt=await rpc(url,"eth_getTransactionReceipt",[txHash]),tx=await rpc(url,"eth_getTransactionByHash",[txHash]);const parsed=parseEthereumTransfer(receipt,tx,c.token_identifier,c.recipient_address);let finality="confirmed";try{const f=await rpc(url,"eth_getBlockByNumber",["finalized",false]);if(f?.number&&receipt?.blockNumber&&BigInt(receipt.blockNumber)<=BigInt(f.number))finality="finalized";}catch{}return {network_code:c.network_code,tx_hash:txHash,asset_code:c.asset_code,...parsed,finality_status:finality,verifier_source:"ethereum_json_rpc_sandbox",observed_at:now()};}
if(c.network_code==="TRON"){const h={"content-type":"application/json",...headers(p)};const info=await fetchJson(`${url}/walletsolidity/gettransactioninfobyid`,{method:"POST",headers:h,body:JSON.stringify({value:txHash})});const tx=await fetchJson(`${url}/wallet/gettransactionbyid`,{method:"POST",headers:h,body:JSON.stringify({value:txHash,visible:true})});const parsed=await parseTronTransfer(info,tx,c.token_identifier,c.recipient_address);return {network_code:c.network_code,tx_hash:txHash,asset_code:c.asset_code,...parsed,finality_status:"solidified",verifier_source:"trongrid_nile_sandbox",observed_at:now()};}
const t=await rpc(url,"getTransaction",[txHash,{encoding:"jsonParsed",commitment:"finalized",maxSupportedTransactionVersion:0}]);const parsed=parseSolanaTransfer(t,c.token_identifier,c.recipient_address);return {network_code:c.network_code,tx_hash:txHash,asset_code:c.asset_code,...parsed,finality_status:"finalized",verifier_source:"solana_json_rpc_sandbox",observed_at:now()};}

export async function fixtureSelfTest(){const ethRecipient="0xbcd27864ea603643bc8aebb3fe2cec2ffdb39eb9",ethToken="0x1c7D4B196Cb0C7B01d743Fbc6116a902379C7238";const eth=parseEthereumTransfer({status:"0x1",blockNumber:"0x10",logs:[{address:ethToken,topics:[TRANSFER_TOPIC,"0x"+"0".repeat(64),"0x"+lowerHex(ethRecipient).padStart(64,"0")],data:"0x2710"}]},{from:"0x1111111111111111111111111111111111111111"},ethToken,ethRecipient);const sol=parseSolanaTransfer({slot:10,meta:{err:null,preTokenBalances:[],postTokenBalances:[{accountIndex:1,mint:"4zMMC9srt5Ri5X14GAgXhaHii3GnPAEERYPJgZJDncDU",owner:"EkNNjreEnhvigAnxY7kL2po3SaVXicCk1CLFyJkkv55F",uiTokenAmount:{amount:"10000"}}]},transaction:{message:{accountKeys:[{pubkey:"Sender111111111111111111111111111111111"}]}}},"4zMMC9srt5Ri5X14GAgXhaHii3GnPAEERYPJgZJDncDU","EkNNjreEnhvigAnxY7kL2po3SaVXicCk1CLFyJkkv55F");return {ethereum:eth.amount_base_units==="10000"&&eth.execution_success,solana:sol.amount_base_units==="10000"&&sol.execution_success,tron_base58:(await tronAddressHex("TKvGfxac4bpFVjdif9vVGoUENBHkidR1WA")).startsWith("41"),version:VERIFIER_VERSION};}

if(import.meta.main)Deno.serve(async req=>{const origin=req.headers.get("origin")||"";if(req.method==="OPTIONS")return origin===ORIGIN?new Response(null,{status:204,headers:cors(origin)}:new Response(null,{status:403});if(origin!==ORIGIN)return Response.json({ok:false,error:"Origin not allowed"},{status:403});const url=Deno.env.get("SUPABASE_URL")||"",service=Deno.env.get("SUPABASE_SERVICE_ROLE_KEY")||"",anon=Deno.env.get("SUPABASE_ANON_KEY")||"";const token=(req.headers.get("Authorization")||"").replace(/^Bearer\s+/i,"").trim();if(!url||!service||!anon||!token)return reply({ok:false,error:"Authentication required"},401,origin);const admin=createClient(url,service,{auth:{persistSession:false,autoRefreshToken:false}});const {data:userData}=await admin.auth.getUser(token);if(!userData?.user)return reply({ok:false,error:"Authentication required"},401,origin);const userClient=createClient(url,anon,{auth:{persistSession:false,autoRefreshToken:false},global:{headers:{Authorization:`Bearer ${token}`}}});const {data:isAdmin}=await userClient.rpc("crypto_is_admin");if(!isAdmin)return reply({ok:false,error:"Admin access required"},403,origin);try{if(req.method==="GET"){const {data,error}=await userClient.rpc("get_crypto_admin_onchain_sandbox_readiness");if(error)throw error;return reply({ok:true,readiness:data,verifier_version:VERIFIER_VERSION},200,origin);}const body=await req.json();if(body.action==="health"){const {data:profiles,error}=await admin.from("crypto_onchain_verifier_profiles").select("*").eq("environment","sandbox").order("network_code");if(error)throw error;const results=[];for(const p of (profiles||[]) as Profile[]){const r=await checkHealth(p);const {error:saveError}=await admin.rpc("service_record_crypto_onchain_verifier_health",{p_network:p.network_code,p_environment:p.environment,p_result:r});if(saveError)throw saveError;results.push({network_code:p.network_code,...r});}return reply({ok:true,results,production_touched:false},200,origin);}if(body.action==="fixture_self_test"){const started=now(),fixtures=await fixtureSelfTest(),ok=fixtures.ethereum&&fixtures.solana&&fixtures.tron_base58;const {data:cases}=await admin.from("crypto_onchain_sandbox_cases").select("id,network_code").eq("environment","sandbox");for(const c of cases||[])await admin.rpc("service_record_crypto_onchain_sandbox_run",{p_case_id:c.id,p_result:{status:ok?"fixture_pass":"fixture_fail",verifier_version:VERIFIER_VERSION,normalized_observation:{fixture_network:c.network_code,fixtures},started_at:started,completed_at:now(),error_code:ok?null:"FIXTURE_FAILED"}});return reply({ok,fixtures,production_touched:false},ok?200:500,origin);}if(body.action==="verify_sandbox_transaction"){const caseId=clean(body.case_id,60),txHash=clean(body.tx_hash,100);const {data:c,error:ce}=await admin.from("crypto_onchain_sandbox_cases").select("*").eq("id",caseId).single();if(ce||!c)return reply({ok:false,error:"Sandbox case not found"},404,origin);const {data:p,error:pe}=await admin.from("crypto_onchain_verifier_profiles").select("*").eq("network_code",c.network_code).eq("environment","sandbox").single();if(pe||!p)throw pe;const started=now();try{const observation=await verifyCase(p as Profile,c as SandboxCase,txHash);const passed=observation.execution_success&&observation.recipient_address===c.recipient_address&&observation.token_identifier===c.token_identifier&&observation.amount_base_units===String(c.expected_amount_base_units)&&["finalized","solidified"].includes(observation.finality_status);const {data:run,error}=await admin.rpc("service_record_crypto_onchain_sandbox_run",{p_case_id:c.id,p_result:{status:passed?"passed":"failed",tx_hash:txHash,verifier_version:VERIFIER_VERSION,normalized_observation:observation,started_at:started,completed_at:now(),error_code:passed?null:"OBSERVATION_MISMATCH"}});if(error)throw error;return reply({ok:passed,observation,run,production_touched:false,entitlement_changed:false},passed?200:409,origin);}catch(e){const code=clean(e instanceof Error?e.message:e);await admin.rpc("service_record_crypto_onchain_sandbox_run",{p_case_id:c.id,p_result:{status:"failed",tx_hash:txHash,verifier_version:VERIFIER_VERSION,normalized_observation:{},started_at:started,completed_at:now(),error_code:code}});return reply({ok:false,error:code,production_touched:false,entitlement_changed:false},400,origin);}}return reply({ok:false,error:"Unsupported action"},400,origin);}catch(e){console.error("crypto-lab-v79-chain-verifier",e);return reply({ok:false,error:"Chain verifier temporarily unavailable"},500,origin);}});
